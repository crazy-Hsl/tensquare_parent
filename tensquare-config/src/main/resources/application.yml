spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
         # uri: https://gitee.com
  rabbitmq:
    host: 192.168.184.135

server:
  port: 12000
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh